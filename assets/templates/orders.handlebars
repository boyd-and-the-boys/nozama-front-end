{{#each orders}}
  {{#if isComplete}}
    <span class="order" data-id={{_id}}>
      <p>Order on {{dateOrdered}}</p>
    </span>
  {{/if}}
{{/each}}
<div id='back-products-button'>Back to Products</div>
